bug in evolution href when multiple tables
adjust base tables
make table scrollable so the headers remain in screen

is thead for accessibility? thead vs scope
aria tag in href?

is reactivness really needed?

alt ok image of pokemon.name or full pic description??